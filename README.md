# Big Data. Blockchain
To get started using Nix run:
```bash
nix develop
```

You can also install [nix-direnv](https://github.com/nix-community/nix-direnv) to automagically enter development environment on cd into project directory.
